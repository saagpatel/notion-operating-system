# DOC-RECONCILIATION.md

This file was generated by the `/doc-truth-up` documentation-reconciliation pass, which treats the
code as read-only ground truth and edits only documentation so it reflects the repo's actual current
state. It changed no source code, config, tests, or lockfiles, and ran no builds or test suites.
Evidence was gathered by reading source files, manifests, and git history only.

---

## Per-Claim Findings

### 1. What it is

**Status:** `consistent`
**Evidence:** `verified-by-reading-code`

README accurately describes the system as connecting Notion, GitHub, and Vercel through dry-run-first
governance pipelines. `src/notion/local-portfolio-actuation.ts` implements all three supported Vercel
action types (`vercel.redeploy`, `vercel.rollback`, `vercel.promote`), and
`config/local-portfolio-governance-policies.json` contains enabled policies for each.
`package.json` describes the project as a "GitHub-installable TypeScript toolkit for safe Notion
publishing plus advanced repo-specific operating workflows."

---

### 2. Current state

**Status:** `drifted` (fixed in `CLAUDE.md`)
**Evidence:** `verified-by-reading-code`

`docs/notion-roadmap.md` (Updated: 2026-05-30) unambiguously states:
- Current Phase: **12 — Managed Write Reliability and Daily Driver Hardening**, Status: Active
- Phases 1–11 complete; Phase 9 (Provider Expansion) marked Completed
- Latest Metrics: 117 total projects, 6 orphaned projects

`CLAUDE.md` (both the "### Current state" block under section 9 and the `<!-- portfolio-context -->`
block) contained four stale claims repeated verbatim in both locations:

| Stale claim | Correct value | Source |
|---|---|---|
| "Phase 9 (Provider Expansion) is planned." | Phase 9 is complete; Phase 12 is active | `docs/notion-roadmap.md` lines 6–7, 300 |
| "Provider scaffolding exists for Vercel (disabled governance policies)" | Vercel policies are enabled in `config/local-portfolio-governance-policies.json` lines 116–162 | `src/notion/local-portfolio-actuation.ts` line 41 |
| "65 projects tracked" | 117 projects tracked | `docs/notion-roadmap.md` Latest Metrics |
| "50 orphaned projects" | 6 orphaned projects | `docs/notion-roadmap.md` Latest Metrics |

**Changes made:**
- `CLAUDE.md` "### Current state" block (section 9): replaced stale four-bullet block with accurate phase, Vercel status, and current metrics.
- `CLAUDE.md` "## Current State" block (Portfolio Context): same replacement.

---

### 3. Stack

**Status:** `consistent`
**Evidence:** `verified-by-reading-code`

`package.json` confirms: TypeScript (`typescript ^6.0.3`), Node ≥ 20, `@notionhq/client ^5.22.0`,
`zod ^4.4.3`, `gray-matter ^4.0.3`, `exceljs ^4.4.0`, `@modelcontextprotocol/sdk 1.29.0`.
Build: `tsc -p tsconfig.json`. CLI binary: `notion-os` at `./dist/src/cli.js`.
README and CLAUDE.md stack descriptions match the manifests.

One gap: CLAUDE.md's "What the repo does between them" (step 5) listed only GitHub writes; Vercel
writes use the same pipeline and are fully implemented. Fixed by adding step 6.

---

### 4. How to run

**Status:** `drifted` (fixed in `README.md` and `CLAUDE.md`)
**Evidence:** `verified-by-reading-code`

**README.md — duplicate section numbers:**
`README.md` has eight numbered `Main Workflows` sections (1–8). Sections for "Governed Vercel actions"
and "Weekly live sequence" were labelled `### 7.` and `### 8.` respectively, duplicating numbers
already used by "Sync GitHub signals" and "Governed GitHub actions (dry-run → approval → live)."
Fixed by renaming to `### 9.` and `### 10.`.

**CLAUDE.md — six non-existent npm scripts in Common Commands table:**
The following scripts appeared in CLAUDE.md's "Common Commands" table but are absent from
`package.json`:

| Script | Status |
|---|---|
| `portfolio-audit:overhaul-notion` | not in package.json |
| `portfolio-audit:execution-views-validate` | not in package.json |
| `portfolio-audit:external-signal-views-validate` | not in package.json |
| `portfolio-audit:phase6-views-validate` | not in package.json |
| `portfolio-audit:phase7-views-validate` | not in package.json |
| `portfolio-audit:phase8-views-validate` | not in package.json |

All six rows were removed from the table.

All other commands in the README and CLAUDE.md tables were verified present in `package.json`.

---

### 5. Known risks

**Status:** `consistent`
**Evidence:** `verified-by-reading-code`

Safety defaults in README and CLAUDE.md (dry-run first, no hardcoded tokens, governed writes only,
additive posture, manual compensation) are consistent with the implementation. `package.json` scripts
default to dry-run; `--live` is an explicit opt-in throughout the codebase.

---

### 6. Next move

**Status:** `consistent`
**Evidence:** `verified-by-reading-code`

`docs/notion-roadmap.md` (Updated: 2026-05-30) documents the next step clearly as Phase 12 packet
follow-through (DevToolsTranslator / Nocturne stale-task queue). README and CLAUDE.md give evergreen
operator guidance (run `governance:health-report` first, dry-run before live) that remains accurate.
No fix required.

---

## Contradictions for Manual Review

### `docs/notion-roadmap.md`

This file is authoritative state but is outside the editable set. No edits needed — it is already
correct and up to date as of 2026-05-30. The drifts were in CLAUDE.md, not here.

### `CLAUDE.md` Purpose section (line 6) — minor omission, not edited

The sentence "**GitHub** is an external signal source and a governed action target" does not mention
Vercel as a governed action target. Since Phase 9 is complete and Vercel actuation is live, a future
author may want to add:

> **Vercel** is also a governed action target — redeploy, rollback, and promote actions flow through
> the same dry-run → approval → live pipeline as GitHub mutations.

This was not edited because the surrounding paragraph focuses on GitHub specifically; the Vercel
addition was captured in the "What the repo does between them" step 6 and the corrected Current State
blocks instead. Manual edit recommended at `CLAUDE.md:6`.

---

## Footer

**Run date/time:** 2026-05-30 21:01:03 PDT  
**Branch:** `docs/truth-up-2026-05-30`  
**HEAD sha reconciled against:** `724154aced40498b258c5db52ca62e464516561a`
