# DOC-RECONCILIATION

This file was generated by the `/doc-truth-up` documentation-reconciliation pass, which treats the code as read-only ground truth and edits only documentation so it reflects the repo's actual current state — it changed no source code, config, tests, or lockfiles, and ran no builds or test suites. Evidence was gathered by reading source files, manifests, and git history only.

---

## Per-Claim Findings

### 1. What it is

**Status:** `consistent`
**Evidence:** `verified-by-reading-code`

`README.md` and `CLAUDE.md` both describe the system as a local automation layer connecting Notion, GitHub, and Vercel through config-driven, dry-run-first CLI workflows. The `package.json` (`bin.notion-os`), `src/cli.ts` entry point, `src/notion/` workflow modules (control-tower, governance, actuation, signals), and `config/` policy files confirm this architecture. No changes made.

---

### 2. Current state

**Status:** `consistent`
**Evidence:** `verified-by-reading-code` (test file count) / `unverifiable-because-project-management-state` (phase and project counts)

- **Test file count:** `tests/*.test.ts` glob yields exactly 59 files, consistent with `docs/notion-roadmap.md:54` ("59 test files and 403 tests" as of 2026-05-31). Individual test counts require running `npm test` to confirm; file count matches.
- **Phase 12 active:** Stated in `docs/notion-roadmap.md:6-8` and `CLAUDE.md`. Phase completion status is not derivable from code structure alone; marked `unverifiable`, but all doc files agree.
- **117 projects, 6 orphaned:** Notion runtime data, not inferable from code. `unverifiable`.
- **No significant stubs or TODOs** found in active CLI/workflow paths.

No changes made.

---

### 3. Stack

**Status:** `consistent`
**Evidence:** `verified-by-reading-code`

`package.json` confirms: TypeScript (`typescript ^6.0.3`), Node ≥ 20, `tsx ^4.22.3`, Vitest `4.1.7`, `@notionhq/client ^5.22.0`, `zod ^4.4.3`, `gray-matter ^4.0.3`, `exceljs ^4.4.0`, `@modelcontextprotocol/sdk 1.29.0`. ESM module type. CLI binary `notion-os` at `./dist/src/cli.js`. README and CLAUDE.md stack descriptions match the manifests. No changes made.

---

### 4. How to run

**Status:** `consistent`
**Evidence:** `verified-by-reading-code`

Every `npm run <script>` command in the README "Main Workflows" and "Common Commands" sections and the CLAUDE.md "Build / Test / Run" section was verified against `package.json`. All referenced scripts exist and route through `src/cli.ts`. Legacy `portfolio-audit:*` aliases are present and resolve to the canonical CLI commands. No changes made.

---

### 5. Known risks / Environment variables

**Status:** `drifted` — fixed in two files
**Evidence:** `verified-by-reading-code` (`src/config/runtime-config.ts:11-44`)

`src/config/runtime-config.ts` defines the canonical list of supported env vars. Two documentation files were out of sync:

**`README.md`** — missing `NOTION_DESTINATIONS_PATH`:
- Before: Environment Variables table ended at `NOTION_LOG_DIR`
- After: added row `| \`NOTION_DESTINATIONS_PATH\` | Optional | Destinations config path (default: \`./config/destinations.json\`) |`

**`CLAUDE.md`** — missing `VERCEL_TOKEN`:
- Before: table went from `GITHUB_APP_WEBHOOK_SECRET` directly to `NOTION_LOG_DIR`
- After: added row `| \`VERCEL_TOKEN\` | Governed Vercel writes | Vercel API token |` between those two rows

All other documented env vars (`NOTION_TOKEN`, `GITHUB_TOKEN`, `GITHUB_APP_ID`, `GITHUB_APP_PRIVATE_KEY_PEM`, `GITHUB_APP_WEBHOOK_SECRET`, `NOTION_LOG_DIR`, `NOTION_DESTINATIONS_PATH`) confirmed present in `runtime-config.ts`. Undocumented optional vars (`NOTION_PROFILE`, `GOOGLE_CALENDAR_TOKEN`, `VERCEL_WEBHOOK_SECRET`, `GITHUB_BREAK_GLASS_TOKEN`, `VERCEL_BREAK_GLASS_TOKEN`, `NOTION_RETRY_MAX_ATTEMPTS`, `NOTION_HTTP_TIMEOUT_MS`) were not added — they are internal tunables not part of the operator-facing surface documented in the README.

---

### 6. Next move

**Status:** `unverifiable`
**Evidence:** `unverifiable-because-forward-looking-project-management`

`docs/notion-roadmap.md:384-387` describes Phase 12 focus as packet follow-through (DevToolsTranslator/Nocturne stale-task queue) and managed write convergence. CLAUDE.md gives evergreen operator guidance (dry-run before live, `governance:health-report` first). Both are operational directives not verifiable from code structure. No changes made.

---

## Contradictions for Manual Review

None. All drifted content was in `README.md` and `CLAUDE.md`, which are within the editable scope for this pass.

---

## Footer

**Reconciliation timestamp:** 2026-06-02 19:34:39 PDT
**Branch:** `docs/truth-up-2026-06-02`
**HEAD sha reconciled against:** `39930e3136a35c477fd70e4546aefd1f752d8ffe`
