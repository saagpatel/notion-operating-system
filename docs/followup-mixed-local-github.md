# Executive summary

The current master and batch artifacts establish the baseline for this bucket: `ContentEngine`, `FreeLanceInvoice`, and `StatusPage` came out of batch-02 with one bounded product slice complete and separate follow-up still open; `CryptForge` came out of batch-07 with remote truth clarified but GitHub and path follow-up still open; `Construction` and `RealEstate` came out of batch-08 with dependency recovery already complete but preserved local branches still behind `origin/main`.

Using those artifacts as source of truth, plus bounded GitHub clarification only where the docs explicitly allow it, two projects can now be treated as complete for this bucket: `FreeLanceInvoice` and `StatusPage`. `ContentEngine` is down to one merged-main workflow proof, `CryptForge` is down to fresh post-fix nightly evidence and an explicit defer-or-move decision on the risky folder rename, and both `Construction` and `RealEstate` still require branch reconciliation before any fresh proof can count.

# Project-by-project plan

| project | already_completed_slice | remaining_local_work | remaining_github_work | current_blocker | recommended_order | minimum_verification | done_signal |
|---|---|---|---|---|---|---|---|
| ContentEngine | Batch-02 already completed the export-and-settings hardening slice with focused Rust and frontend proof; the broader release/docs lane has since been pushed through the finish branch and merged PR follow-up, so the remaining question is proof, not new product scope. | None unless the merged-main workflow exposes a real regression; do not reopen the product slice preemptively. | Let the March 24, 2026 merged-main `desktop-ci` run finish; only do targeted repair if that run fails. Keep any future dependency noise separate from this proof lane. | The merged-main `desktop-ci` run is still in progress, so the release/docs closeout is landed but not fully re-proven yet. | 1. Wait for the current merged-main `desktop-ci` result. 2. If it fails, fix only the surfaced issue and rerun that proof. 3. After that, keep any later dependency noise in a separate hygiene pass. | `quality-gates` on the March 24, 2026 merge is already green, and the paired `desktop-ci` run for that same merge must complete successfully. | PR `#4` is merged, no open PR backlog remains, and the March 24, 2026 merged-main `desktop-ci` proof is green. |
| FreeLanceInvoice | Batch-02 already completed the invoice payment-link safety slice with passing Rust proof; the broader release-candidate closeout and dependency-PR cleanup have since landed, and the old open-PR blocker is gone. | None for this bucket. Keep the product lane closed unless a new regression appears. | None active for this bucket; only watch for fresh bot noise if it appears later. | None for this bucket. | 1. Keep the repo closed for this bucket. 2. Treat any future PR or workflow churn as new work, not unfinished carryover. | Existing proof plus merged finish PR and zero open PRs are enough; no extra repo-wide rerun is needed from this packet. | The payment-link slice is already proven, the finish PR is merged, the stale dependency PR lane is gone, and no active GitHub blocker remains. |
| StatusPage | Batch-02 already completed the internal-support recent-activity slice with focused web-test proof; the later dependency PR follow-up is merged and the product lane is no longer open. | None for this bucket. Do not reopen the product slice. | None active for this bucket; historical failed counts are superseded by current green mainline results. | None for this bucket. | 1. Keep product scope closed. 2. Ignore historical workflow counts unless a new current failure appears. | Green `CI` and `quality-gates` on the March 24, 2026 merge, plus zero open PRs, are sufficient proof. | The bounded support-console slice is complete, the dependency PR lane is cleared, and the current mainline signal is green. |
| CryptForge | Batch-07 already established that `origin` is canonical, the current branch compares to `origin/main`, and `npm run verify:preflight` passes; the follow-up PR lane has since been resolved and the Windows `doctor-env` fix has been merged. | No required product work remains. The folder rename should stay deferred unless there is a clearly lossless move window with a clean enough worktree. | Wait for fresh post-fix `ci-nightly` evidence on `main`; do not treat the March 22-23 failures as current product blockers, but do require one post-fix nightly or equivalent Windows smoke result before calling the GitHub lane clear. | The latest completed `ci-nightly` evidence is still the pre-fix failure, so the merged Windows fix has not been re-proven by the scheduled lane yet. | 1. Get fresh `ci-nightly` or equivalent post-fix Windows proof. 2. Once that is green, explicitly decide to keep the folder rename deferred or perform it in a separate lossless move window. 3. Do not mix the folder move with workflow repair. | A green post-fix `ci-nightly` or equivalent Windows cross-platform smoke result, plus the already-passing local preflight and zero open PRs. | The Windows nightly lane is green after the fix, no open PR debt remains, and the folder rename is either explicitly deferred or completed without risk. |
| Construction | Batch-08 already preserved the restored dependency baseline, cleared the original Dependabot backlog, and kept the local dirty work intact; later finish-lane CI fixes are already merged, so the remaining work is local branch reconciliation, not baseline recovery. | Reconcile the preserved dirty branch onto `origin/main` without dropping local work, then rerun the next finish-level verify slice from that reconciled branch. | A new Dependabot PR `#4` is open, but it is post-batch hygiene noise and should stay separate from the branch-reconciliation lane until the local branch is safe. | The preserved dirty local branch is still behind `origin/main`, so fresh proof from the unreconciled branch would not count as a finish signal. | 1. Reconcile the dirty branch onto `origin/main`. 2. Rerun the next finish-level verify slice from the reconciled state. 3. Only after that, merge or close the new Dependabot PR `#4` as separate hygiene. | Successful no-loss reconciliation plus fresh proof from the reconciled branch; historical failed workflow counts from the old dependency lane do not matter unless a new current run fails. | The preserved branch is no longer behind `origin/main`, fresh reconciled-branch proof passes, and PR `#4` is either resolved or intentionally left as separate future hygiene. |
| RealEstate | Batch-08 already preserved the restored dependency baseline, cleared the PR backlog, and reduced recent workflow noise to zero; later finish-lane merge proof is green on `main`, so the remaining work is the preserved-branch reconcile step. | Reconcile the preserved dirty branch onto `origin/main` without dropping local work, then rerun the next finish-level verify slice from that reconciled branch. | None active after the current bucket work; keep GitHub quiet while the local reconcile step happens. | The preserved dirty local branch is still `4` commits behind `origin/main`, so current green `main` proof does not replace reconciled-branch proof. | 1. Reconcile the dirty branch onto `origin/main`. 2. Rerun the next finish-level verify slice from the reconciled state. 3. Keep any new bot noise separate unless it becomes current and material. | Successful no-loss reconciliation plus fresh proof from the reconciled branch; current green `Quality` and `quality-gates` on `main` are supporting evidence, not the final local-branch closeout. | The preserved branch is no longer behind `origin/main`, fresh reconciled-branch proof passes, and the repo stays at zero active PR and workflow noise. |

# Recommended execution order

1. `ContentEngine`: finish the one remaining merged-main proof check before reopening anything else.
2. `Construction`: reconcile the preserved dirty branch onto `origin/main`, then capture fresh proof before touching the new Dependabot PR.
3. `RealEstate`: reconcile the preserved dirty branch onto `origin/main`, then capture fresh proof.
4. `CryptForge`: get fresh post-fix nightly proof, then explicitly defer or safely perform the folder move in a separate lane.
5. Keep `FreeLanceInvoice` and `StatusPage` closed unless new evidence creates a genuinely new blocker.

# Cross-project cautions

- Keep finished product slices separate from GitHub hygiene. `FreeLanceInvoice` and `StatusPage` should not be reopened just because older batch records once carried PR or workflow counts.
- For `Construction` and `RealEstate`, branch reconciliation must happen before fresh proof. Green `main` runs are helpful context, but they do not close the preserved dirty branch.
- For `ContentEngine`, do not start another local cleanup pass while the merged-main `desktop-ci` run is still unresolved. The next move depends on that result.
- For `CryptForge`, the folder move is the risky part. It should not be combined with workflow repair, PR cleanup, or any other finish work.
- Historical failed-workflow counts from the source docs should stay historical unless the latest current run still points to the same blocker.

# Done definition

- `FreeLanceInvoice` and `StatusPage` remain closed for this bucket because their completed slices are already proven and no active GitHub blocker remains.
- `ContentEngine` is done when the March 24, 2026 merged-main `desktop-ci` run completes green, with no need to reopen product scope unless that run fails.
- `Construction` is done when the preserved dirty branch is reconciled onto `origin/main`, fresh reconciled-branch proof passes, and the new PR `#4` is then handled as separate hygiene.
- `RealEstate` is done when the preserved dirty branch is reconciled onto `origin/main` and fresh reconciled-branch proof passes while the repo stays quiet.
- `CryptForge` is done when the merged Windows nightly fix has fresh post-fix proof and the folder rename is explicitly deferred or completed in a separate lossless move.
