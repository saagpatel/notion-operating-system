# 1. Executive summary

The GitHub/workflow/governance bucket for the five scoped projects is materially smaller now than the March 24 planning snapshot. `ScreenshotAnnotate`, `prompt-englab`, and `AIGCCore` are now verified complete for this bucket because their scoped GitHub, workflow, governance, and Notion follow-through items were closed after the plan was executed.

Only two projects still have real remaining work in this bucket. `DevToolsTranslator` still has an intentionally open governed issue for active release-readiness blockers on `main`, and `JobCommandCenter` still has an intentionally open governed finish-validation slice tied to the current default-branch posture. `NOTION_TOKEN` is no longer a blocker for this bucket because the scoped Notion refreshes were completed.

# 2. Project-by-project plan

| project | already_proven | remaining_issue_type | current_blocker | exact_closure_action | dependencies | minimum_verification | notion_dependency | done_signal |
|---|---|---|---|---|---|---|---|---|
| ScreenshotAnnotate | Local proof was already green, the remaining Dependabot PR was resolved, the governed issue was closed, and the scoped Notion row was refreshed. | none | None for this bucket. | No further GitHub, workflow, governance, or Notion action is needed for this bucket. | None. | Verify there are no open PRs or issues and no new blocker has replaced the closed governed lane. | Notion refresh already completed; no auth blocker remains. | ScreenshotAnnotate has no open PRs, no open issues, and no remaining scoped GitHub/governance blocker. |
| DevToolsTranslator | The scratch-state ambiguity was removed, recent `release-extension-stage-controller` runs on `main` succeeded on March 23 and March 24, 2026, the governed issue text was reconciled to current reality, and the scoped Notion row was refreshed. | governed execution reconcile plus release-readiness gate | Issue `#1` remains open for the current release-readiness slice: manual Chrome sign-off, Chrome Web Store credentials, updater-signature inputs, and one unresolved `perf-reliability-regression` failure from March 21, 2026. | 1. Clear or replace the blockers tracked in issue `#1`. 2. Reconcile the issue body if fresher evidence changes the blocker slice. 3. Close the governed issue once release-readiness blockers are resolved. | Blocked by current GitHub and governance state, not by local baseline or Notion auth. | 1. Verify the blocker list in issue `#1` matches live workflow evidence. 2. Verify the unresolved release blocker is either fixed or replaced with fresher evidence-backed blockers. 3. Verify issue `#1` closes when the slice is no longer active. | Notion refresh already completed; no auth blocker remains. | The governed issue closes because the active release-readiness blocker slice on `main` is no longer open. |
| JobCommandCenter | The stale lockfile mismatch was repaired, `npm run build` passes, the Python sidecar dry-run path was verified, PR `#2` was closed, the default branch was intentionally kept as `polish/v1.0-improvements`, issue `#3` was rewritten as the active finish slice, and the scoped Notion row was refreshed. | governed execution finish slice plus default-branch decision | Issue `#3` remains open for finish validation: PyInstaller bundle proof, real 5-job batch evidence, LinkedIn bot-detection uncertainty, and the need to keep the current default-branch posture intentional until validation is complete. | 1. Validate the PyInstaller bundle. 2. Run a real 5-job batch and capture evidence. 3. Document LinkedIn bot-detection behavior. 4. Revisit the default-branch posture only after finish validation evidence exists. 5. Close issue `#3` when the finish slice is complete. | Blocked by current GitHub and governance state, plus an intentional default-branch decision; not blocked by Notion auth. | 1. Verify the finish evidence exists for the PyInstaller bundle and a real 5-job batch. 2. Verify LinkedIn bot-detection behavior is documented well enough to remove uncertainty. 3. Verify issue `#3` closes only after the default-branch posture is still intentional or is explicitly changed with evidence. | Notion refresh already completed; no auth blocker remains. | Issue `#3` closes and the default-branch posture is explicitly justified by finish-validation evidence rather than placeholder workflow activity. |
| prompt-englab | The release-readiness path was repaired, `release:verify:readiness` passed, the two `legacy-origin` tracking branches were cleared, `legacy-origin` was removed, the stale GitHub issue was closed, and the scoped Notion row was refreshed. | none | None for this bucket. | No further GitHub, workflow, governance, or Notion action is needed for this bucket. | None. | Verify `release:verify:readiness` remains green if rerun, `git branch -vv` no longer shows `legacy-origin` tracking, and GitHub still has no open PRs or issues for this slice. | Notion refresh already completed; no auth blocker remains. | prompt-englab has no remaining scoped release-readiness, remote-governance, PR, or issue blocker. |
| AIGCCore | The workflow repair was pushed, `codex-quality-security` reran successfully, the open PR and issue surface is now clear for this slice, and the scoped Notion row was refreshed. | none | None for this bucket. | No further GitHub, workflow, governance, or Notion action is needed for this bucket. | None. | Verify the repaired `codex-quality-security` run is still the current truth and no open PR or issue has replaced the resolved workflow blocker. | Notion refresh already completed; no auth blocker remains. | AIGCCore no longer has the `dependency_and_misconfig` workflow blocker and has no remaining scoped GitHub/governance follow-up for this bucket. |

# 3. Recommended execution order

1. JobCommandCenter
2. DevToolsTranslator

The only remaining execution work in this bucket is the governed finish slice in `JobCommandCenter` and the governed release-readiness slice in `DevToolsTranslator`. `JobCommandCenter` is first because its remaining work mixes finish proof with an intentional default-branch posture, while `DevToolsTranslator` is narrower and can follow once the release blockers in issue `#1` are the main remaining governed lane.

# 4. Cross-project risks

- The biggest risk now is stale planning language: older batch artifacts still describe all five projects as open, but that is no longer true after the work that was completed.
- `JobCommandCenter` can create accidental churn if the default branch is flipped before finish validation evidence exists.
- `DevToolsTranslator` can drift again if the governed issue stops matching the live blocker slice on `main`.
- Historical failed workflow runs in GitHub for `prompt-englab` and older `AIGCCore` runs are now background noise, not active blockers for this bucket.
- `NOTION_TOKEN` is no longer the blocker for this scoped bucket because the relevant Notion refreshes already ran successfully.

# 5. Done definition

- `docs/followup-github-governance.md` and `docs/followup-github-governance-summary.json` both describe the same five-project bucket state after the completed execution work.
- `verified_complete` contains `ScreenshotAnnotate`, `prompt-englab`, and `AIGCCore` because their scoped GitHub, workflow, governance, and Notion follow-through items are closed.
- `DevToolsTranslator` is only done for this bucket when issue `#1` no longer needs to carry the active release-readiness blocker slice.
- `JobCommandCenter` is only done for this bucket when issue `#3` closes and the default-branch posture is still intentional or explicitly revised based on finish-validation evidence.
- Solved local baseline work stays solved unless new evidence shows an actual regression.
